olc_seeds_decoder.zip contains 1000 randomly selected valid inputs for OLC decoder.
These are named seed0001.txt to seed1000.txt
Perhaps take the first 100 if only 100 are needed

Similarly olc_seeds_decode_cluster.zip contains 1000 unique valid inputs for OLC decoder.
These are grouped into widely separated clusters.

Similarly olc_seeds_decode_finecluster.zip contains 1000 unique valid inputs for OLC decoder.
Again data are clustered but within clusters they represent places that are even closer.
Opps bug so first seed in olc_seeds_decode_finecluster.zip is not valid
